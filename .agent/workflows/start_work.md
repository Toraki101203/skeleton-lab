---
description: Start work session by syncing with remote repository and ensuring environment is ready
---
1. Pull the latest changes from the remote repository
git pull

2. Install dependencies to match the updated package.json (if changed)
npm install
