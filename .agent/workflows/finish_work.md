---
description: Finish work session by pushing changes to GitHub
---
1. Check the current status of the repository
git status

2. Stage all changes
git add .

3. Commit changes with a descriptive message. (Agent: Generate a concise summary of the session's work for the commit message)
git commit -m "Your descriptive commit message here"

4. Push the changes to the remote repository
git push
